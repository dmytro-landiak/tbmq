---
phase: 05-websocket-transport
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ws/MqttWsServerBootstrap.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ws/MqttWsChannelInitializer.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wss/MqttWssServerBootstrap.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wss/MqttWssChannelInitializer.java
  - lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttWsIntegrationTest.java
  - lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttWssIntegrationTest.java
  - lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/AbstractMqttIntegrationTest.java
autonomous: true
requirements:
  - TRAN-03
  - TRAN-04
  - TRAN-05

must_haves:
  truths:
    - "An MQTT client connects over ws:// on port 8084 and publishes/subscribes successfully"
    - "An MQTT client connects over wss:// on port 8085 using TLS with the same mounted server certificate"
    - "The broker responds with Sec-WebSocket-Protocol: mqtt in the WebSocket handshake response"
    - "WS and TCP listeners coexist — a client can connect via TCP and another via WS simultaneously"
    - "WSS is disabled by default and only starts when tbmq.tls.enabled=true"
  artifacts:
    - path: "lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ws/MqttWsServerBootstrap.java"
      provides: "WebSocket server bootstrap"
      contains: "extends AbstractServerBootstrap"
    - path: "lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ws/MqttWsChannelInitializer.java"
      provides: "WebSocket channel pipeline"
      contains: "WebSocketServerProtocolHandler"
    - path: "lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wss/MqttWssServerBootstrap.java"
      provides: "Secure WebSocket server bootstrap"
      contains: "extends AbstractServerBootstrap"
    - path: "lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wss/MqttWssChannelInitializer.java"
      provides: "Secure WebSocket channel pipeline with SslHandler first"
      contains: "sslHandlerProvider.createSslHandler"
    - path: "lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttWsIntegrationTest.java"
      provides: "WS transport integration tests"
      contains: "ws://127.0.0.1"
    - path: "lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttWssIntegrationTest.java"
      provides: "WSS transport integration tests"
      contains: "wss://127.0.0.1"
  key_links:
    - from: "MqttWsChannelInitializer"
      to: "MqttSessionHandlerFactory"
      via: "sessionHandlerFactory.create()"
      pattern: "sessionHandlerFactory\\.create\\(\\)"
    - from: "MqttWsChannelInitializer"
      to: "WebSocketServerProtocolHandler"
      via: "pipeline addLast with subprotocols"
      pattern: "WebSocketServerProtocolHandler.*mqttv3\\.1,mqtt"
    - from: "MqttWssChannelInitializer"
      to: "MqttSslHandlerProvider"
      via: "createSslHandler for WSS TLS"
      pattern: "sslHandlerProvider\\.createSslHandler"
    - from: "MqttWsIntegrationTest"
      to: "ws:// broker URL"
      via: "Paho WebSocket transport"
      pattern: "ws://127\\.0\\.0\\.1"
    - from: "MqttWssIntegrationTest"
      to: "wss:// broker URL"
      via: "Paho WebSocket Secure transport"
      pattern: "wss://127\\.0\\.0\\.1"
---

<objective>
Add WebSocket (WS) and secure WebSocket (WSS) transport bootstraps, channel initializers, and integration tests.

Purpose: This plan delivers the actual WS/WSS listeners and verifies them end-to-end, completing TRAN-03, TRAN-04, and TRAN-05. Browser-based MQTT clients (MQTT.js, Paho JavaScript) can connect over ws:// and wss:// after this plan.

Output: MqttWsServerBootstrap, MqttWsChannelInitializer, MqttWssServerBootstrap, MqttWssChannelInitializer, MqttWsIntegrationTest, MqttWssIntegrationTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-websocket-transport/05-CONTEXT.md
@.planning/phases/05-websocket-transport/05-RESEARCH.md
@.planning/phases/05-websocket-transport/05-01-SUMMARY.md

@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/AbstractServerBootstrap.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/MqttSessionHandlerFactory.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/MqttTcpServerBootstrap.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/MqttChannelInitializer.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/tls/MqttSslServerBootstrap.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/tls/MqttSslChannelInitializer.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/tls/MqttSslHandlerProvider.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ConnectionCountHandler.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wshandler/WsBinaryFrameHandler.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wshandler/WsByteBufEncoder.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wshandler/WsContinuationFrameHandler.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wshandler/WsTextFrameHandler.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/WsConfiguration.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/WssConfiguration.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/MqttConfiguration.java
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/TlsConfiguration.java
@lightweight/src/main/resources/tbmq-lightweight.yml
@lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/AbstractMqttIntegrationTest.java
@lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttTlsIntegrationTest.java

<interfaces>
<!-- From Plan 01 outputs (created in Wave 1) -->

From AbstractServerBootstrap:
```java
public abstract class AbstractServerBootstrap implements SmartLifecycle {
    protected AbstractServerBootstrap(NettyConfiguration nettyConfig);
    protected abstract int getPort();
    protected abstract ChannelInitializer<SocketChannel> getChannelInitializer();
    protected abstract String getServerName();
    public int getLocalPort();
    // start(), stop(), isRunning() are concrete
}
```

From MqttSessionHandlerFactory:
```java
@Component
public class MqttSessionHandlerFactory {
    public MqttSessionHandler create();
}
```

From MqttSslHandlerProvider:
```java
@Service
public class MqttSslHandlerProvider {
    public SslHandler createSslHandler(SocketChannel channel);
    // SslContext is null when tbmq.tls.enabled=false
}
```

From WsConfiguration:
```java
@Configuration @Data
public class WsConfiguration {
    boolean enabled;     // default true
    int port;            // default 8084
    String subProtocols; // default "mqttv3.1,mqtt"
    int maxContentLength; // default 65536
}
```

From WssConfiguration:
```java
@Configuration @Data
public class WssConfiguration {
    boolean enabled;     // default false
    int port;            // default 8085
    String subProtocols; // default "mqttv3.1,mqtt"
    int maxContentLength; // default 65536
}
```

From AbstractMqttIntegrationTest:
```java
@SpringBootTest(properties = {"spring.config.name=tbmq-lightweight", "tbmq.netty.port=0", ...})
public abstract class AbstractMqttIntegrationTest {
    @Autowired protected MqttTcpServerBootstrap mqttServer;
    protected String brokerUrl();          // tcp://127.0.0.1:{port}
    protected MqttClient createClient(String clientId);
    protected MqttConnectOptions defaultConnectOptions();  // includes tbmq/tbmq creds
}
```

From MqttTlsIntegrationTest:
```java
@SpringBootTest(properties = {..., "tbmq.tls.enabled=true", "tbmq.tls.port=0", "tbmq.tls.cert-path=classpath:tls/server.pem", ...})
class MqttTlsIntegrationTest extends AbstractMqttIntegrationTest {
    @Autowired MqttSslServerBootstrap tlsServer;
    protected SSLSocketFactory createTrustingSocketFactory();
}
```

Pipeline order from CONTEXT.md D-06:
WS:  idle -> connectionCount -> HttpServerCodec -> HttpObjectAggregator(65536) -> WebSocketServerProtocolHandler("/mqtt", "mqttv3.1,mqtt") -> WsBinaryFrameHandler -> WsContinuationFrameHandler -> WsTextFrameHandler -> WsByteBufEncoder -> MqttDecoder -> MqttEncoder -> MqttSessionHandler
WSS: ssl -> idle -> connectionCount -> [same as WS from HttpServerCodec onwards]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WS and WSS bootstraps and channel initializers</name>
  <files>
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ws/MqttWsServerBootstrap.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ws/MqttWsChannelInitializer.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wss/MqttWssServerBootstrap.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wss/MqttWssChannelInitializer.java
  </files>
  <read_first>
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/AbstractServerBootstrap.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/MqttSessionHandlerFactory.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/MqttTcpServerBootstrap.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/MqttChannelInitializer.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/tls/MqttSslServerBootstrap.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/tls/MqttSslChannelInitializer.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/tls/MqttSslHandlerProvider.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ConnectionCountHandler.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wshandler/WsBinaryFrameHandler.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wshandler/WsByteBufEncoder.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wshandler/WsContinuationFrameHandler.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wshandler/WsTextFrameHandler.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/WsConfiguration.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/WssConfiguration.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/MqttConfiguration.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/TlsConfiguration.java,
    application/src/main/java/org/thingsboard/mqtt/broker/server/AbstractMqttWsChannelInitializer.java,
    application/src/main/java/org/thingsboard/mqtt/broker/server/ws/MqttWsServerBootstrap.java,
    application/src/main/java/org/thingsboard/mqtt/broker/server/ws/MqttWsChannelInitializer.java,
    application/src/main/java/org/thingsboard/mqtt/broker/server/wss/MqttWssServerBootstrap.java,
    application/src/main/java/org/thingsboard/mqtt/broker/server/wss/MqttWssChannelInitializer.java
  </read_first>
  <action>
    **Part A: Create MqttWsChannelInitializer (per D-06)**

    Create `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ws/MqttWsChannelInitializer.java`:

    Package: `org.thingsboard.mqtt.broker.lightweight.server.ws`

    ```java
    @Slf4j
    @Component
    @RequiredArgsConstructor
    public class MqttWsChannelInitializer extends ChannelInitializer<SocketChannel> {

        private static final String WS_PATH = "/mqtt";

        private final ConnectionCountHandler connectionCountHandler;
        private final MqttSessionHandlerFactory sessionHandlerFactory;
        private final MqttConfiguration mqttConfig;
        private final WsConfiguration wsConfig;

        @Override
        protected void initChannel(SocketChannel ch) {
            ch.pipeline()
                .addLast("idle", new IdleStateHandler(0, 0, 0))
                .addLast("connectionCount", connectionCountHandler)
                // HTTP/WebSocket upgrade pipeline
                .addLast(new HttpServerCodec())
                .addLast(new HttpObjectAggregator(wsConfig.getMaxContentLength()))
                .addLast(new WebSocketServerProtocolHandler(WS_PATH, wsConfig.getSubProtocols()))
                // WebSocket frame handlers (decode WS frames to ByteBuf for MQTT decoder)
                .addLast(new WsBinaryFrameHandler())
                .addLast(new WsContinuationFrameHandler())
                .addLast(new WsTextFrameHandler())
                .addLast(new WsByteBufEncoder())
                // MQTT codec
                .addLast("decoder", new MqttDecoder(mqttConfig.getMaxPayloadSize(), mqttConfig.getMaxClientIdLength()))
                .addLast("encoder", MqttEncoder.INSTANCE)
                .addLast("handler", sessionHandlerFactory.create());
        }
    }
    ```

    Imports needed: `io.netty.handler.codec.http.HttpObjectAggregator`, `io.netty.handler.codec.http.HttpServerCodec`, `io.netty.handler.codec.http.websocketx.WebSocketServerProtocolHandler`, `io.netty.handler.codec.mqtt.MqttDecoder`, `io.netty.handler.codec.mqtt.MqttEncoder`, `io.netty.handler.timeout.IdleStateHandler`, plus all four wshandler classes.

    CRITICAL: `WebSocketServerProtocolHandler` constructor takes `(String websocketPath, String subprotocols)`. The `subprotocols` string is `"mqttv3.1,mqtt"` (per D-04). Netty automatically handles `Sec-WebSocket-Protocol` header negotiation — when a client requests `mqtt`, Netty echoes it back in the response (TRAN-05).

    **Part B: Create MqttWsServerBootstrap (per D-02, D-03)**

    Create `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ws/MqttWsServerBootstrap.java`:

    Package: `org.thingsboard.mqtt.broker.lightweight.server.ws`

    ```java
    @Slf4j
    @Service
    public class MqttWsServerBootstrap extends AbstractServerBootstrap {

        private final WsConfiguration wsConfig;
        private final MqttWsChannelInitializer channelInitializer;

        public MqttWsServerBootstrap(NettyConfiguration nettyConfig,
                                     WsConfiguration wsConfig,
                                     MqttWsChannelInitializer channelInitializer) {
            super(nettyConfig);
            this.wsConfig = wsConfig;
            this.channelInitializer = channelInitializer;
        }

        @Override
        public int getPhase() {
            return 2;  // Per D-03: TCP=0, TLS=1, WS=2, WSS=3
        }

        @Override
        public boolean isAutoStartup() {
            return wsConfig.isEnabled();
        }

        @Override
        protected int getPort() {
            return wsConfig.getPort();
        }

        @Override
        protected ChannelInitializer<SocketChannel> getChannelInitializer() {
            return channelInitializer;
        }

        @Override
        protected String getServerName() {
            return "MQTT WS";
        }

        @Override
        public void start() {
            if (!wsConfig.isEnabled()) {
                log.info("WebSocket listener is disabled (tbmq.ws.enabled=false), skipping startup");
                return;
            }
            super.start();
        }
    }
    ```

    **Part C: Create MqttWssChannelInitializer (per D-06, D-01)**

    Create `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wss/MqttWssChannelInitializer.java`:

    Package: `org.thingsboard.mqtt.broker.lightweight.server.wss`

    Same as WS channel initializer EXCEPT:
    - Inject `MqttSslHandlerProvider sslHandlerProvider` additionally
    - In `initChannel()`, the FIRST handler added is `"ssl"` -> `sslHandlerProvider.createSslHandler(ch)`, BEFORE the `"idle"` handler
    - Use `WssConfiguration` instead of `WsConfiguration` for port/subprotocols/maxContentLength

    ```java
    @Slf4j
    @Component
    @RequiredArgsConstructor
    public class MqttWssChannelInitializer extends ChannelInitializer<SocketChannel> {

        private static final String WS_PATH = "/mqtt";

        private final MqttSslHandlerProvider sslHandlerProvider;
        private final ConnectionCountHandler connectionCountHandler;
        private final MqttSessionHandlerFactory sessionHandlerFactory;
        private final MqttConfiguration mqttConfig;
        private final WssConfiguration wssConfig;

        @Override
        protected void initChannel(SocketChannel ch) {
            ch.pipeline()
                // SSL MUST be first — decrypts before anything else processes
                .addLast("ssl", sslHandlerProvider.createSslHandler(ch))
                .addLast("idle", new IdleStateHandler(0, 0, 0))
                .addLast("connectionCount", connectionCountHandler)
                // HTTP/WebSocket upgrade pipeline
                .addLast(new HttpServerCodec())
                .addLast(new HttpObjectAggregator(wssConfig.getMaxContentLength()))
                .addLast(new WebSocketServerProtocolHandler(WS_PATH, wssConfig.getSubProtocols()))
                // WebSocket frame handlers
                .addLast(new WsBinaryFrameHandler())
                .addLast(new WsContinuationFrameHandler())
                .addLast(new WsTextFrameHandler())
                .addLast(new WsByteBufEncoder())
                // MQTT codec
                .addLast("decoder", new MqttDecoder(mqttConfig.getMaxPayloadSize(), mqttConfig.getMaxClientIdLength()))
                .addLast("encoder", MqttEncoder.INSTANCE)
                .addLast("handler", sessionHandlerFactory.create());
        }
    }
    ```

    CRITICAL per D-01: WSS reuses `MqttSslHandlerProvider` from Phase 4. Do NOT create a separate WSS SSL handler provider.

    **Part D: Create MqttWssServerBootstrap (per D-02, D-03)**

    Create `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wss/MqttWssServerBootstrap.java`:

    Package: `org.thingsboard.mqtt.broker.lightweight.server.wss`

    ```java
    @Slf4j
    @Service
    public class MqttWssServerBootstrap extends AbstractServerBootstrap {

        private final WssConfiguration wssConfig;
        private final TlsConfiguration tlsConfig;
        private final MqttWssChannelInitializer channelInitializer;

        public MqttWssServerBootstrap(NettyConfiguration nettyConfig,
                                      WssConfiguration wssConfig,
                                      TlsConfiguration tlsConfig,
                                      MqttWssChannelInitializer channelInitializer) {
            super(nettyConfig);
            this.wssConfig = wssConfig;
            this.tlsConfig = tlsConfig;
            this.channelInitializer = channelInitializer;
        }

        @Override
        public int getPhase() {
            return 3;  // Per D-03: TCP=0, TLS=1, WS=2, WSS=3
        }

        @Override
        public boolean isAutoStartup() {
            // WSS requires BOTH wss.enabled=true AND tls.enabled=true (per Pitfall 2 from RESEARCH.md)
            return wssConfig.isEnabled() && tlsConfig.isEnabled();
        }

        @Override
        protected int getPort() {
            return wssConfig.getPort();
        }

        @Override
        protected ChannelInitializer<SocketChannel> getChannelInitializer() {
            return channelInitializer;
        }

        @Override
        protected String getServerName() {
            return "MQTT WSS";
        }

        @Override
        public void start() {
            if (!wssConfig.isEnabled()) {
                log.info("Secure WebSocket listener is disabled (tbmq.wss.enabled=false), skipping startup");
                return;
            }
            if (!tlsConfig.isEnabled()) {
                log.warn("WSS is enabled but TLS is not configured (tbmq.tls.enabled=false). WSS listener cannot start without TLS certificates.");
                return;
            }
            super.start();
        }
    }
    ```

    CRITICAL per Pitfall 2 (RESEARCH.md): `isAutoStartup()` checks BOTH `wssConfig.isEnabled()` AND `tlsConfig.isEnabled()`. The `start()` method has an explicit guard with warning log for the case where WSS is enabled but TLS is not.
  </action>
  <verify>
    <automated>cd /home/dlandiak/projects/gsd/tbmq/lightweight && mvn compile -pl . 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ws/MqttWsServerBootstrap.java` exists and contains `extends AbstractServerBootstrap`
    - MqttWsServerBootstrap contains `getPhase()` returning `2`
    - MqttWsServerBootstrap contains `getServerName()` returning `"MQTT WS"`
    - File `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ws/MqttWsChannelInitializer.java` exists
    - MqttWsChannelInitializer contains `new WebSocketServerProtocolHandler(WS_PATH, wsConfig.getSubProtocols())`
    - MqttWsChannelInitializer contains `new WsBinaryFrameHandler()`
    - MqttWsChannelInitializer contains `new WsContinuationFrameHandler()`
    - MqttWsChannelInitializer contains `new WsTextFrameHandler()`
    - MqttWsChannelInitializer contains `new WsByteBufEncoder()`
    - MqttWsChannelInitializer contains `sessionHandlerFactory.create()`
    - MqttWsChannelInitializer pipeline order has `HttpServerCodec` BEFORE `MqttDecoder`
    - File `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wss/MqttWssServerBootstrap.java` exists and contains `extends AbstractServerBootstrap`
    - MqttWssServerBootstrap contains `getPhase()` returning `3`
    - MqttWssServerBootstrap `isAutoStartup()` contains `wssConfig.isEnabled() && tlsConfig.isEnabled()`
    - MqttWssServerBootstrap `start()` contains `log.warn` about TLS not configured
    - File `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wss/MqttWssChannelInitializer.java` exists
    - MqttWssChannelInitializer pipeline starts with `"ssl"` handler (addLast("ssl", sslHandlerProvider.createSslHandler(ch)))
    - MqttWssChannelInitializer contains `sslHandlerProvider.createSslHandler`
    - MqttWssChannelInitializer does NOT create its own SSL context (reuses MqttSslHandlerProvider per D-01)
    - `mvn compile -pl lightweight` succeeds
  </acceptance_criteria>
  <done>WS bootstrap on SmartLifecycle phase 2, WSS on phase 3. WS channel initializer has correct HTTP/WS/MQTT pipeline order per D-06. WSS prepends SslHandler as first handler. WSS requires both wss.enabled and tls.enabled. Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create WS and WSS integration tests + update base test class</name>
  <files>
    lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttWsIntegrationTest.java,
    lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttWssIntegrationTest.java,
    lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/AbstractMqttIntegrationTest.java
  </files>
  <read_first>
    lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/AbstractMqttIntegrationTest.java,
    lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttTlsIntegrationTest.java,
    lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttConnectIntegrationTest.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/ws/MqttWsServerBootstrap.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/server/wss/MqttWssServerBootstrap.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/WsConfiguration.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/WssConfiguration.java
  </read_first>
  <action>
    **Part A: Update AbstractMqttIntegrationTest to add WS port property**

    Add `"tbmq.ws.port=0"` to the existing `@SpringBootTest(properties = {...})` annotation in `AbstractMqttIntegrationTest`. This allocates a random port for WS in the base test context, enabling WS tests that extend the base.

    Also add `"tbmq.wss.enabled=false"` to ensure WSS remains disabled in the base test context (it defaults to false, but explicit is safer for test clarity).

    The updated properties list should be:
    ```java
    @SpringBootTest(properties = {
        "spring.config.name=tbmq-lightweight",
        "tbmq.netty.port=0",
        "tbmq.netty.max-connections=100",
        "tbmq.storage.rocksdb.path=${java.io.tmpdir}/tbmq-test-mqtt",
        "tbmq.ws.port=0",
        "tbmq.wss.enabled=false",
        "management.server.port=0"
    })
    ```

    **Part B: Create MqttWsIntegrationTest (covers TRAN-03 and TRAN-05)**

    Create `lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttWsIntegrationTest.java`:

    This test extends `AbstractMqttIntegrationTest` (inherits the base @SpringBootTest context which now includes `tbmq.ws.port=0`).

    ```java
    package org.thingsboard.mqtt.broker.lightweight.mqtt;

    import org.eclipse.paho.client.mqttv3.MqttClient;
    import org.eclipse.paho.client.mqttv3.MqttConnectOptions;
    import org.eclipse.paho.client.mqttv3.MqttException;
    import org.eclipse.paho.client.mqttv3.MqttMessage;
    import org.eclipse.paho.client.mqttv3.persist.MemoryPersistence;
    import org.junit.jupiter.api.Test;
    import org.springframework.beans.factory.annotation.Autowired;
    import org.thingsboard.mqtt.broker.lightweight.server.ws.MqttWsServerBootstrap;

    import java.util.concurrent.CountDownLatch;
    import java.util.concurrent.TimeUnit;
    import java.util.concurrent.atomic.AtomicReference;

    import static org.assertj.core.api.Assertions.assertThat;
    ```

    Class: `class MqttWsIntegrationTest extends AbstractMqttIntegrationTest`

    Inject: `@Autowired private MqttWsServerBootstrap wsServer;`

    Helper method `wsBrokerUrl()`: returns `"ws://127.0.0.1:" + wsServer.getLocalPort() + "/mqtt"`.
    CRITICAL per Pitfall 6 (RESEARCH.md): The URL MUST include `/mqtt` path. Paho sends the HTTP upgrade to this path. Without it, WebSocketServerProtocolHandler returns HTTP 400.

    Helper method `createWsClient(String clientId)`: creates `new MqttClient(wsBrokerUrl(), clientId, new MemoryPersistence())`, adds to `clients` list, returns client.

    Tests:

    1. **`testWsConnect_thenConnackAccepted`** (TRAN-03 + TRAN-05):
       - Create WS client with `createWsClient("ws-connect-1")`
       - Connect with `defaultConnectOptions()`
       - Assert `client.isConnected() == true`
       - NOTE: If `Sec-WebSocket-Protocol: mqtt` was NOT echoed, Paho would throw `MqttException` at `connect()`. So a successful connect implicitly verifies TRAN-05.

    2. **`testWsPublishSubscribe_thenMessageDelivered`** (TRAN-03):
       - Create subscriber WS client "ws-sub-1", connect, subscribe to "ws/test/topic" QoS 1 with callback that captures received message
       - Create publisher WS client "ws-pub-1", connect, publish "hello-ws" to "ws/test/topic" QoS 1
       - Use `CountDownLatch` with 5-second timeout to wait for message delivery
       - Assert received message payload equals "hello-ws"
       - Assert received topic equals "ws/test/topic"

    3. **`testWsAndTcpCoexist_thenBothWork`** (TRAN-03):
       - Create TCP client using `createClient("tcp-coexist-1")`, connect via TCP
       - Create WS client using `createWsClient("ws-coexist-1")`, connect via WS
       - TCP client subscribes to "coexist/topic" QoS 1 with message callback
       - WS client publishes "cross-transport" to "coexist/topic" QoS 1
       - Assert TCP subscriber receives the message published by WS client
       - This verifies that TCP and WS transports feed into the same dispatch service

    **Part C: Create MqttWssIntegrationTest (covers TRAN-04)**

    Create `lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttWssIntegrationTest.java`:

    This test uses a SEPARATE Spring context (like MqttTlsIntegrationTest) because WSS requires TLS to be enabled. Do NOT extend AbstractMqttIntegrationTest's @SpringBootTest annotation — provide its own.

    ```java
    package org.thingsboard.mqtt.broker.lightweight.mqtt;

    // Same imports as MqttWsIntegrationTest PLUS:
    import org.bouncycastle.jce.provider.BouncyCastleProvider;
    import org.springframework.boot.test.context.SpringBootTest;
    import org.springframework.test.annotation.DirtiesContext;
    import org.thingsboard.mqtt.broker.lightweight.server.wss.MqttWssServerBootstrap;
    import javax.net.ssl.SSLContext;
    import javax.net.ssl.SSLSocketFactory;
    import javax.net.ssl.TrustManagerFactory;
    import java.io.InputStream;
    import java.security.KeyStore;
    import java.security.Security;
    import java.security.cert.Certificate;
    import java.security.cert.CertificateFactory;
    ```

    Use its own `@SpringBootTest` annotation with TLS enabled:
    ```java
    @SpringBootTest(properties = {
        "spring.config.name=tbmq-lightweight",
        "tbmq.netty.port=0",
        "tbmq.netty.max-connections=100",
        "tbmq.storage.rocksdb.path=${java.io.tmpdir}/tbmq-test-wss",
        "tbmq.ws.port=0",
        "tbmq.tls.enabled=true",
        "tbmq.tls.port=0",
        "tbmq.tls.cert-path=classpath:tls/server.pem",
        "tbmq.tls.key-path=classpath:tls/server-key.pem",
        "tbmq.tls.client-auth=NONE",
        "tbmq.wss.enabled=true",
        "tbmq.wss.port=0",
        "management.server.port=0"
    })
    @DirtiesContext(classMode = DirtiesContext.ClassMode.AFTER_CLASS)
    ```

    Class: `class MqttWssIntegrationTest extends AbstractMqttIntegrationTest`

    Inject: `@Autowired private MqttWssServerBootstrap wssServer;`

    Helper method `wssBrokerUrl()`: returns `"wss://127.0.0.1:" + wssServer.getLocalPort() + "/mqtt"`. CRITICAL: include `/mqtt` path.

    Helper method `createTrustingSocketFactory()`: Copy the exact implementation from `MqttTlsIntegrationTest.createTrustingSocketFactory()` — loads `tls/ca.pem` from classpath, builds `SSLContext` with TrustManagerFactory, returns `SSLSocketFactory`.

    Helper method `createWssClient(String clientId)`: creates `new MqttClient(wssBrokerUrl(), clientId, new MemoryPersistence())`, adds to `clients`, returns.

    Helper method `wssConnectOptions(SSLSocketFactory socketFactory)`: creates `MqttConnectOptions` with `cleanSession=true`, `connectionTimeout=10`, `keepAliveInterval=30`, `userName="tbmq"`, `password="tbmq"`, `socketFactory=socketFactory`.

    Tests:

    1. **`testWssConnect_thenConnackAccepted`** (TRAN-04):
       - Create WSS client with `createWssClient("wss-connect-1")`
       - Get `SSLSocketFactory` from `createTrustingSocketFactory()`
       - Connect with `wssConnectOptions(socketFactory)`
       - Assert `client.isConnected() == true`

    2. **`testWssPublishSubscribe_thenMessageDelivered`** (TRAN-04):
       - Same pattern as WS pub/sub test but over WSS
       - Subscriber WSS client subscribes to "wss/test/topic"
       - Publisher WSS client publishes "hello-wss"
       - Assert message delivered with correct payload

    3. **`testWssAndWsCoexist_thenBothWork`** (TRAN-04):
       - Create WS client (inject `MqttWsServerBootstrap wsServer` additionally)
       - Connect both WS and WSS clients
       - WS subscriber receives message from WSS publisher
       - Proves both transports work simultaneously
  </action>
  <verify>
    <automated>cd /home/dlandiak/projects/gsd/tbmq/lightweight && mvn test -pl . -Dtest="MqttWsIntegrationTest,MqttWssIntegrationTest" -Dsurefire.failIfNoSpecifiedTests=false 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttWsIntegrationTest.java` exists
    - MqttWsIntegrationTest contains `ws://127.0.0.1` (WS broker URL)
    - MqttWsIntegrationTest contains `/mqtt` in the URL (path for WebSocket upgrade)
    - MqttWsIntegrationTest contains `testWsConnect_thenConnackAccepted`
    - MqttWsIntegrationTest contains `testWsPublishSubscribe_thenMessageDelivered`
    - MqttWsIntegrationTest contains `testWsAndTcpCoexist_thenBothWork`
    - File `lightweight/src/test/java/org/thingsboard/mqtt/broker/lightweight/mqtt/MqttWssIntegrationTest.java` exists
    - MqttWssIntegrationTest contains `wss://127.0.0.1` (WSS broker URL)
    - MqttWssIntegrationTest contains `tbmq.tls.enabled=true` in @SpringBootTest properties
    - MqttWssIntegrationTest contains `tbmq.wss.enabled=true` in @SpringBootTest properties
    - MqttWssIntegrationTest contains `tbmq.wss.port=0` in @SpringBootTest properties
    - MqttWssIntegrationTest contains `createTrustingSocketFactory`
    - MqttWssIntegrationTest contains `testWssConnect_thenConnackAccepted`
    - MqttWssIntegrationTest contains `testWssPublishSubscribe_thenMessageDelivered`
    - AbstractMqttIntegrationTest @SpringBootTest properties contain `tbmq.ws.port=0`
    - `mvn test -pl lightweight -Dtest="MqttWsIntegrationTest,MqttWssIntegrationTest"` passes with 0 failures
    - `mvn test -pl lightweight` (full suite) passes with 0 failures
  </acceptance_criteria>
  <done>WS integration tests verify connect, pub/sub, and TCP coexistence over ws://. WSS integration tests verify connect and pub/sub over wss:// with TLS certs. Paho's WebSocket handshake validation implicitly verifies Sec-WebSocket-Protocol: mqtt header (TRAN-05). All existing tests continue to pass alongside new WS/WSS tests.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd lightweight && mvn test -pl .` — ALL tests pass (existing + new WS/WSS)
2. `mvn test -pl lightweight -Dtest="MqttWsIntegrationTest"` — WS tests pass independently
3. `mvn test -pl lightweight -Dtest="MqttWssIntegrationTest"` — WSS tests pass independently
4. Verify TRAN-03: `grep -c "ws://127.0.0.1" lightweight/src/test/java/.../MqttWsIntegrationTest.java` — WS connections tested
5. Verify TRAN-04: `grep -c "wss://127.0.0.1" lightweight/src/test/java/.../MqttWssIntegrationTest.java` — WSS connections tested
6. Verify TRAN-05: Paho `connect()` succeeds over WS (Paho validates Sec-WebSocket-Protocol header internally — if missing, throws HandshakeFailedException)
7. SmartLifecycle phases: `grep "getPhase" lightweight/src/main/java/.../ws/MqttWsServerBootstrap.java` returns 2, `grep "getPhase" lightweight/src/main/java/.../wss/MqttWssServerBootstrap.java` returns 3
</verification>

<success_criteria>
- WS bootstrap starts on port 8084 (or random port in tests) at SmartLifecycle phase 2
- WSS bootstrap starts on port 8085 (or random port in tests) at SmartLifecycle phase 3, only when both wss.enabled AND tls.enabled are true
- WS channel pipeline follows D-06 order exactly: idle -> connectionCount -> HTTP -> WS -> frame handlers -> MQTT codec -> session handler
- WSS channel pipeline has SslHandler as first handler, then same as WS
- MQTT clients can connect, publish, and subscribe over ws:// (TRAN-03)
- MQTT clients can connect, publish, and subscribe over wss:// (TRAN-04)
- Sec-WebSocket-Protocol: mqtt is echoed (TRAN-05, verified implicitly by Paho handshake)
- TCP/TLS/WS/WSS transports all coexist and feed into the same dispatch service
- All integration tests (existing + new) pass with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-websocket-transport/05-02-SUMMARY.md`
</output>
