---
phase: 02-core-protocol-mqtt-3-1-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lightweight/pom.xml
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorMsg.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/MsgType.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorId.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbTypeActorId.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActor.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/AbstractTbActor.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorRef.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorCtx.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorCreator.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorSystem.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorSystemSettings.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorMailbox.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/DefaultTbActorSystem.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/Dispatcher.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorException.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorNotRegisteredException.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/InitFailureStrategy.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/ProcessFailureStrategy.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/ActorSystemConfiguration.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/MqttConfiguration.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/session/ClientSessionCtx.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/session/DisconnectReasonType.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/session/SessionState.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/service/mqtt/PublishMsg.java
  - lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/packet/PacketIdAllocator.java
  - lightweight/src/main/resources/tbmq-lightweight.yml
autonomous: true
requirements:
  - PROTO-01
  - PROTO-07

must_haves:
  truths:
    - "Actor system starts as SmartLifecycle at phase -1 (after RocksDB, before Netty)"
    - "Actor framework can create, lookup, and destroy actors with ConcurrentLinkedQueue mailbox"
    - "Actor messages are processed sequentially within a single actor (no lock contention)"
    - "Session context holds channel reference, session state, and QoS tracking maps"
    - "DisconnectReasonType distinguishes LWT-triggering from non-triggering disconnects"
  artifacts:
    - path: "lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/DefaultTbActorSystem.java"
      provides: "Actor system with dispatcher and mailbox management"
      min_lines: 150
    - path: "lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorMailbox.java"
      provides: "Per-actor message queue with AtomicBoolean busy flag"
      min_lines: 100
    - path: "lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/ActorSystemConfiguration.java"
      provides: "SmartLifecycle bean at phase -1 with ForkJoinPool dispatcher"
      contains: "getPhase"
    - path: "lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/session/ClientSessionCtx.java"
      provides: "Per-client session context with channel, state, QoS maps"
      min_lines: 40
  key_links:
    - from: "ActorSystemConfiguration"
      to: "DefaultTbActorSystem"
      via: "SmartLifecycle bean wiring"
      pattern: "new DefaultTbActorSystem"
    - from: "TbActorMailbox"
      to: "Dispatcher"
      via: "executor submission for message processing"
      pattern: "dispatcher.*getExecutor"
---

<objective>
Port the TBMQ actor framework into the lightweight project and create all session/protocol infrastructure types needed by subsequent plans.

Purpose: Every MQTT message flows through the actor-per-client model (D-01). The actor framework provides sequential per-client processing without locks. Session types define the contract for connect, disconnect, QoS tracking, and client takeover. This plan establishes all foundational types so Plans 03-05 can build protocol handlers against stable interfaces.

Output: Actor framework (~19 Java files), actor system Spring configuration, session context, disconnect reason types, MQTT configuration properties, packet ID allocator, publish message POJO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-protocol-mqtt-3-1-1/02-CONTEXT.md
@.planning/phases/02-core-protocol-mqtt-3-1-1/02-RESEARCH.md

@lightweight/pom.xml
@lightweight/src/main/resources/tbmq-lightweight.yml
@lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/NettyConfiguration.java

<interfaces>
<!-- Source files to PORT from TBMQ common/actor into lightweight/actors/ -->
<!-- Read each source file and adapt: change package to o.t.m.b.lightweight.actors, -->
<!-- remove ActorStatsManager dependency, inline ThingsBoardExecutors utilities, -->
<!-- simplify to single dispatcher per D-03. -->

From common/actor/src/main/java/.../actors/TbActorSystem.java (57 lines):
  interface TbActorSystem {
    ScheduledExecutorService getScheduler();
    void createDispatcher(String dispatcherId, ExecutorService executor);
    void destroyDispatcher(String dispatcherId);
    TbActorRef getActor(TbActorId actorId);
    TbActorRef createRootActor(String dispatcherId, TbActorCreator creator);
    TbActorRef createChildActor(String dispatcherId, TbActorCreator creator, TbActorId parent);
    void tell(TbActorId target, TbActorMsg actorMsg);
    void tellWithHighPriority(TbActorId target, TbActorMsg actorMsg);
    void stop(TbActorRef actorRef);
    void stop(TbActorId actorId);
    void broadcastToChildren(TbActorId parent, TbActorMsg msg);
    Set<TbActorId> getAllActorIds();
    void destroy();
  }

From common/actor/src/main/java/.../actors/DefaultTbActorSystem.java (224 lines):
  Uses: ConcurrentMap<TbActorId, TbActorMailbox>, ConcurrentMap<String, Dispatcher>
  Dependencies to remove: ActorStatsManager, ThingsBoardExecutors, ThingsBoardThreadFactory
  Dependencies to inline: shutdownAndAwaitTermination (simple ExecutorService.shutdownNow + awaitTermination)

From common/actor/src/main/java/.../actors/TbActorMailbox.java (237 lines):
  ConcurrentLinkedQueue + AtomicBoolean busy flag pattern for lock-free single-threaded execution

From common/actor/src/main/java/.../actors/msg/MsgType.java (78 lines):
  Enum with all message types — SIMPLIFY to Phase 2 types only:
  SESSION_INIT_MSG, CONNECT_MSG, DISCONNECT_MSG, PUBLISH_MSG, SUBSCRIBE_MSG,
  UNSUBSCRIBE_MSG, PING_MSG, PUBACK_MSG, PUBREC_MSG, PUBREL_MSG, PUBCOMP_MSG,
  TAKEOVER_MSG, DELIVER_MSG

From common/actor/src/main/java/.../actors/msg/TbActorMsg.java (31 lines):
  interface TbActorMsg { MsgType getMsgType(); }

From common/util/src/main/java/.../util/ThingsBoardExecutors.java:
  shutdownAndAwaitTermination uses Guava MoreExecutors — inline as:
    executor.shutdown(); executor.awaitTermination(5, SECONDS); executor.shutdownNow();

From common/util/src/main/java/.../util/ThingsBoardThreadFactory.java (57 lines):
  Simple ThreadFactory with named threads — inline into lightweight

From common/util/src/main/java/.../util/ThingsBoardForkJoinWorkerThreadFactory.java (43 lines):
  ForkJoinPool.ForkJoinWorkerThreadFactory with named threads — inline into lightweight
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port actor framework from common/actor into lightweight module</name>
  <files>
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorMsg.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/MsgType.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorId.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbTypeActorId.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActor.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/AbstractTbActor.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorRef.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorCtx.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorCreator.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorSystem.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorSystemSettings.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorMailbox.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/DefaultTbActorSystem.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/Dispatcher.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorException.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorNotRegisteredException.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/InitFailureStrategy.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/ProcessFailureStrategy.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/ActorSystemConfiguration.java,
    lightweight/src/main/resources/tbmq-lightweight.yml
  </files>
  <read_first>
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/TbActorSystem.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/DefaultTbActorSystem.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/TbActorMailbox.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/TbActor.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/AbstractTbActor.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/TbActorRef.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/TbActorCtx.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/TbActorCreator.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/TbActorId.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/TbTypeActorId.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/TbActorSystemSettings.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/TbActorException.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/TbActorNotRegisteredException.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/Dispatcher.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/InitFailureStrategy.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/ProcessFailureStrategy.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/msg/TbActorMsg.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/msg/MsgType.java,
    common/actor/src/main/java/org/thingsboard/mqtt/broker/actors/ActorStatsManager.java,
    common/util/src/main/java/org/thingsboard/mqtt/broker/common/util/ThingsBoardExecutors.java,
    common/util/src/main/java/org/thingsboard/mqtt/broker/common/util/ThingsBoardThreadFactory.java,
    common/util/src/main/java/org/thingsboard/mqtt/broker/common/util/ThingsBoardForkJoinWorkerThreadFactory.java,
    lightweight/src/main/resources/tbmq-lightweight.yml
  </read_first>
  <action>
    Port the entire TBMQ actor framework from `common/actor/` into `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/`. Per D-02, use the battle-tested implementation.

    **Package:** `org.thingsboard.mqtt.broker.lightweight.actors` (flat — no sub-packages needed).

    **Files to port verbatim (change package only):**
    - `TbActorId.java` — interface with `toString()`
    - `TbTypeActorId.java` — `TbActorId` impl with type+id String pair, `equals`/`hashCode` on both fields
    - `TbActor.java` — interface with `init()`, `process()`, `destroy()`, `getActorId()`
    - `AbstractTbActor.java` — abstract base with `TbActorCtx` field
    - `TbActorRef.java` — interface extending `TbActorId` with `tell()`, `tellWithHighPriority()`
    - `TbActorCtx.java` — interface extending `TbActorRef` with `stop()`, `getActorSystem()`
    - `TbActorCreator.java` — functional interface: `createActorId()`, `createActor()`
    - `Dispatcher.java` — record/data class with `dispatcherId` + `ExecutorService executor`
    - `TbActorException.java` — simple RuntimeException subclass
    - `TbActorNotRegisteredException.java` — exception with `TbActorId` field
    - `InitFailureStrategy.java` — enum: `STOP`, `RETRY`
    - `ProcessFailureStrategy.java` — enum with `stop` flag

    **Files to port with modifications:**
    - `TbActorMsg.java` — interface: `MsgType getMsgType()`. Keep as-is.
    - `MsgType.java` — SIMPLIFY enum to Phase 2 types only: `SESSION_INIT_MSG`, `CONNECT_MSG`, `DISCONNECT_MSG`, `PUBLISH_MSG`, `SUBSCRIBE_MSG`, `UNSUBSCRIBE_MSG`, `PING_MSG`, `PUBACK_MSG`, `PUBREC_MSG`, `PUBREL_MSG`, `PUBCOMP_MSG`, `TAKEOVER_MSG`, `DELIVER_MSG`. Remove all TBMQ-specific types (DEVICE_*, INTEGRATION_*, etc.).
    - `TbActorSystemSettings.java` — keep `actorThroughput` (default 10), `schedulerPoolSize` (default 1), `maxActorInitAttempts` (default 10). Use `@Data` + `@Builder`. No Spring annotation — this is a plain POJO, configured by `ActorSystemConfiguration`.
    - `TbActorMailbox.java` (237 lines) — Port verbatim. Key structure: `ConcurrentLinkedQueue<TbActorMsg> normalPriorityMsgs`, `ConcurrentLinkedQueue<TbActorMsg> highPriorityMsgs`, `AtomicBoolean busy`, `StampedLock destroyLock`. Port all logic including `tryProcessQueue()`, `processMailbox()`, `enqueue()`, `destroy()`. Implements `TbActorCtx`, `TbActorRef`.
    - `DefaultTbActorSystem.java` (224 lines) — Port with these changes:
      1. Remove `ActorStatsManager` parameter from constructor (lightweight has no stats manager). Remove `statsManager.registerActorsStats(actors)` call.
      2. Replace `ThingsBoardExecutors.shutdownAndAwaitTermination(executor, name)` with inlined: `executor.shutdown(); try { if (!executor.awaitTermination(5, TimeUnit.SECONDS)) executor.shutdownNow(); } catch (InterruptedException e) { executor.shutdownNow(); Thread.currentThread().interrupt(); }`
      3. Replace `ThingsBoardThreadFactory.forName("actor-system-scheduler")` with inlined `Executors.defaultThreadFactory()` or a simple lambda ThreadFactory: `r -> { Thread t = new Thread(r, "actor-scheduler-" + counter.getAndIncrement()); t.setDaemon(true); return t; }`.
      4. Replace `ThingsBoardExecutors.newWorkStealingPool` references (if used in config) with direct `new ForkJoinPool(parallelism)`.
      5. Keep all locking/concurrent logic exactly as original — this is battle-tested.

    **Create `ActorSystemConfiguration.java`:**
    Package: `org.thingsboard.mqtt.broker.lightweight.config`
    - `@Configuration` + `@RequiredArgsConstructor` + `@Slf4j`
    - Implements `SmartLifecycle` with `getPhase()` returning `-1` (per Pitfall 6: starts after RocksDB at MIN_VALUE, before Netty at 0)
    - `start()`: create `TbActorSystemSettings` (throughput=10, schedulerPoolSize=1, maxInitAttempts=10), instantiate `DefaultTbActorSystem`, create single dispatcher `"client-dispatcher"` backed by `new ForkJoinPool(Runtime.getRuntime().availableProcessors())` per D-03.
    - `stop()`: call `actorSystem.destroy()`
    - Expose `TbActorSystem` via `@Bean` method or field getter so other beans can inject it.
    - Since the bean must be available for injection but `SmartLifecycle.start()` creates it: use `@Bean` that returns the `DefaultTbActorSystem` created eagerly in the constructor, and `start()`/`stop()` manage the dispatcher and scheduler lifecycle.

    **Add YAML configuration for actor system in `tbmq-lightweight.yml`:**
    ```yaml
    tbmq:
      actors:
        throughput: "${TBMQ_ACTOR_THROUGHPUT:10}"
        scheduler-pool-size: "${TBMQ_ACTOR_SCHEDULER_POOL_SIZE:1}"
        max-init-attempts: "${TBMQ_ACTOR_MAX_INIT_ATTEMPTS:10}"
    ```

    **Add YAML configuration for MQTT protocol properties:**
    ```yaml
    tbmq:
      mqtt:
        max-payload-size: "${TBMQ_MQTT_MAX_PAYLOAD_SIZE:268435456}"
        max-client-id-length: "${TBMQ_MQTT_MAX_CLIENT_ID_LENGTH:128}"
        keep-alive-max: "${TBMQ_MQTT_KEEP_ALIVE_MAX:600}"
    ```

    **CRITICAL: Do NOT add Guava dependency.** The original `DefaultTbActorSystem` uses `MoreExecutors.shutdownAndAwaitTermination` from Guava. Inline this as standard JDK `executor.shutdown()` + `awaitTermination()` + `shutdownNow()` fallback. Guava is a heavy dependency unnecessary for this single utility.
  </action>
  <verify>
    <automated>cd /home/dlandiak/projects/gsd/tbmq && mvn -f lightweight/pom.xml compile -q</automated>
  </verify>
  <acceptance_criteria>
    - `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/DefaultTbActorSystem.java` exists and contains `class DefaultTbActorSystem implements TbActorSystem`
    - `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorMailbox.java` exists and contains `ConcurrentLinkedQueue` and `AtomicBoolean`
    - `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/MsgType.java` contains `SESSION_INIT_MSG` and `PUBLISH_MSG` and `TAKEOVER_MSG` but does NOT contain `DEVICE_` or `INTEGRATION_`
    - `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/ActorSystemConfiguration.java` contains `getPhase()` returning `-1`
    - `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/ActorSystemConfiguration.java` contains `client-dispatcher` and `ForkJoinPool`
    - `tbmq-lightweight.yml` contains `tbmq.actors.throughput` and `tbmq.mqtt.max-payload-size` and `tbmq.mqtt.max-client-id-length`
    - No file imports `com.google.common` or `com.google.guava` (no Guava dependency)
    - No file imports from `org.thingsboard.mqtt.broker.actors` (original package) — all use `o.t.m.b.lightweight.actors`
    - `mvn -f lightweight/pom.xml compile -q` exits 0
  </acceptance_criteria>
  <done>All 19 actor framework files compiled, ActorSystemConfiguration at SmartLifecycle phase -1, YAML config properties for actors and MQTT protocol</done>
</task>

<task type="auto">
  <name>Task 2: Create session and protocol infrastructure types</name>
  <files>
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/session/ClientSessionCtx.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/session/DisconnectReasonType.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/session/SessionState.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/service/mqtt/PublishMsg.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/packet/PacketIdAllocator.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/MqttConfiguration.java
  </files>
  <read_first>
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/config/NettyConfiguration.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/TbActorMsg.java,
    lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/MsgType.java,
    application/src/main/java/org/thingsboard/mqtt/broker/actors/client/ClientActor.java,
    application/src/main/java/org/thingsboard/mqtt/broker/service/mqtt/will/DefaultLastWillService.java
  </read_first>
  <action>
    Create all session and protocol types that Plans 03-05 build upon. These are data classes and enums — no business logic.

    **`DisconnectReasonType.java`** — Package: `o.t.m.b.lightweight.session`
    ```java
    public enum DisconnectReasonType {
        ON_DISCONNECT_MSG(false),           // clean DISCONNECT packet
        ON_CONFLICTING_SESSIONS(false),     // client takeover (D-11: suppress LWT)
        ON_KEEP_ALIVE(true),               // keep-alive timeout
        ON_CHANNEL_CLOSED(true),           // unexpected channel close
        ON_PROTOCOL_ERROR(true),           // malformed packet, protocol violation
        ON_PACKET_TOO_LARGE(true),         // exceeds max payload
        ON_MALFORMED_PACKET(true),         // decoder error
        ON_ERROR(true);                     // general error

        private final boolean lastWillAllowed;

        DisconnectReasonType(boolean lastWillAllowed) {
            this.lastWillAllowed = lastWillAllowed;
        }

        public boolean allowsLastWillOnDisconnect() {
            return lastWillAllowed;
        }
    }
    ```
    CRITICAL per Pitfall 1 from RESEARCH: `ON_CONFLICTING_SESSIONS` MUST return `false` from `allowsLastWillOnDisconnect()`.

    **`SessionState.java`** — Package: `o.t.m.b.lightweight.session`
    ```java
    public enum SessionState {
        INITIALIZING,   // channel open, waiting for CONNECT
        CONNECTED,      // CONNACK sent, session active
        DISCONNECTING,  // DISCONNECT received or initiated
        DISCONNECTED    // channel closed
    }
    ```

    **`ClientSessionCtx.java`** — Package: `o.t.m.b.lightweight.session`
    Fields:
    - `UUID sessionId` — unique per connection (not per clientId)
    - `String clientId` — from CONNECT packet
    - `ChannelHandlerContext channel` — Netty channel for writeAndFlush
    - `SessionState state` — volatile, starts at INITIALIZING
    - `boolean cleanSession` — from CONNECT packet
    - `int keepAliveSeconds` — from CONNECT packet
    - `ConcurrentHashMap<Integer, Object> inboundQos2` — packetId -> AWAITING_PUBREL marker (QoS 2 dedup, per Pattern 6 from RESEARCH)
    - `ConcurrentHashMap<Integer, PublishMsg> outboundQos2` — packetId -> message awaiting PUBCOMP
    - `ConcurrentHashMap<Integer, PublishMsg> outboundQos1` — packetId -> message awaiting PUBACK
    - `PacketIdAllocator packetIdAllocator` — per-session allocator instance
    Use `@Getter`, `@Setter` for fields. Constructor takes `(UUID sessionId, ChannelHandlerContext channel)`. State starts at `INITIALIZING`. Allocator created in constructor. Maps created in constructor.

    **`PublishMsg.java`** — Package: `o.t.m.b.lightweight.service.mqtt`
    ```java
    @Data
    @Builder
    public class PublishMsg {
        private final String topicName;
        private final int qos;
        private final byte[] payload;
        private final boolean retain;
        private final boolean dup;
        private final int packetId;
    }
    ```

    **`PacketIdAllocator.java`** — Package: `o.t.m.b.lightweight.packet`
    Per Pattern 7 from RESEARCH. Per-session instance (not shared across clients).
    ```java
    public class PacketIdAllocator {
        private final AtomicInteger counter = new AtomicInteger(0);
        private final Set<Integer> inUse = ConcurrentHashMap.newKeySet();

        public int nextPacketId() {
            for (int i = 0; i < 65535; i++) {
                int id = (counter.incrementAndGet() & 0xFFFF);
                if (id == 0) id = 1; // 0 is not valid per MQTT spec
                if (inUse.add(id)) return id;
            }
            throw new IllegalStateException("No available packet IDs (65535 in-flight)");
        }

        public void releasePacketId(int id) {
            inUse.remove(id);
        }

        public void releaseAll() {
            inUse.clear();
        }
    }
    ```

    **`MqttConfiguration.java`** — Package: `o.t.m.b.lightweight.config`
    ```java
    @Configuration
    @ConfigurationProperties(prefix = "tbmq.mqtt")
    @Data
    public class MqttConfiguration {
        private int maxPayloadSize = 268435456;   // 256 MB default
        private int maxClientIdLength = 128;       // spec SHOULD is 23, but many clients use longer
        private int keepAliveMax = 600;            // max allowed keep-alive in seconds
    }
    ```
  </action>
  <verify>
    <automated>cd /home/dlandiak/projects/gsd/tbmq && mvn -f lightweight/pom.xml compile -q && mvn -f lightweight/pom.xml test -q</automated>
  </verify>
  <acceptance_criteria>
    - `DisconnectReasonType.java` contains `ON_CONFLICTING_SESSIONS(false)` — LWT suppressed for takeover
    - `DisconnectReasonType.java` contains `ON_KEEP_ALIVE(true)` — LWT fires on keep-alive expiry
    - `DisconnectReasonType.java` contains `allowsLastWillOnDisconnect()` method
    - `ClientSessionCtx.java` contains `UUID sessionId`, `String clientId`, `ChannelHandlerContext channel`, `SessionState state`, `ConcurrentHashMap.*inboundQos2`, `ConcurrentHashMap.*outboundQos2`, `PacketIdAllocator`
    - `PacketIdAllocator.java` contains `AtomicInteger counter`, `Set.*inUse`, `nextPacketId()`, `releasePacketId(int id)`
    - `PublishMsg.java` contains `@Builder` and fields: `topicName`, `qos`, `payload`, `retain`
    - `MqttConfiguration.java` contains `@ConfigurationProperties(prefix = "tbmq.mqtt")` and fields `maxPayloadSize`, `maxClientIdLength`, `keepAliveMax`
    - `SessionState.java` contains `INITIALIZING`, `CONNECTED`, `DISCONNECTING`, `DISCONNECTED`
    - `mvn -f lightweight/pom.xml compile -q` exits 0
    - `mvn -f lightweight/pom.xml test -q` exits 0 (existing tests still pass)
  </acceptance_criteria>
  <done>All session/protocol types compile, existing tests pass, DisconnectReasonType correctly distinguishes LWT-triggering vs non-triggering reasons</done>
</task>

</tasks>

<verification>
- `mvn -f lightweight/pom.xml compile -q` exits 0
- `mvn -f lightweight/pom.xml test -q` exits 0 (all existing tests pass)
- Actor framework files exist in `lightweight/src/main/java/org/thingsboard/mqtt/broker/lightweight/actors/`
- No Guava imports in any lightweight source file
- `ActorSystemConfiguration.getPhase()` returns -1
</verification>

<success_criteria>
1. All 19 actor framework files compile under `o.t.m.b.lightweight.actors` package
2. ActorSystemConfiguration creates ForkJoinPool dispatcher at SmartLifecycle phase -1
3. All session/protocol types (ClientSessionCtx, DisconnectReasonType, SessionState, PublishMsg, PacketIdAllocator, MqttConfiguration) compile
4. Existing Phase 1 tests still pass
5. YAML config has actor and MQTT protocol properties
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-protocol-mqtt-3-1-1/02-01-SUMMARY.md`
</output>
